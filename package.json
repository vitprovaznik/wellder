{
  "name": "wellder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/wellder/ && gh-pages -d dist -o origin"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "accordion-js": "^3.4.1",
    "vituum": "^1.2.0"
  }
}
